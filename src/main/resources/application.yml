# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fyblog?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

shiro-redis:
  enabled: true
  redis-manager:
    host: 
    password: 

mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

fy:
  jwt:
    secret: f4e2e52034348f86b67cde581c0f9eb5
    expire: 604800
    header: Authorization
server:
  port: 8081
